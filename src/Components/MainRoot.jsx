import React from 'react';
import auth from '../FireBasePackages/FireBaseConfig.js';

const MainRoot = () => {
    console.log(auth)
    return (
        <div>
            this is main Root
        </div>
    );
};

export default MainRoot;